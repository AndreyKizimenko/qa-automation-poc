FLEET_URL=https://your-loadtest-instance.example.com
FLEET_API_TOKEN=
FLEET_ADMIN_EMAIL=
FLEET_ADMIN_PASSWORD=
FLEET_OBSERVER_EMAIL=
FLEET_OBSERVER_PASSWORD=
# Numeric id of the fleet (team) containing the loadtest dataset. Set per run
# by the workflow that provisions the loadtest team. Required for the loadtest
# project — specs fail at fixture setup if absent or non-numeric.
FLEET_LOADTEST_FLEET_ID=
