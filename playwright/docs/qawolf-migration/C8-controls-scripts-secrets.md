# Controls + Scripts + Secrets + Packages — audit

Cluster: macOS custom settings/profiles-with-labels, setup experience (bootstrap / install-fleetd-manually / setup UI), macOS updates host-count, download script, batch-run scripts + the batch-script-progress-page (tabs/status/preview/cancel/schedule), custom variables (secrets) CRUD + script-secret dependency, package edit advanced attributes + self-service.

## Disposition table

| # | QA Wolf flow (basename) | Tier | Behavior (1 line) | Disposition | Target (existing or proposed path) | Notes |
|---|---|---|---|---|---|---|
| 1 | controls-download-script-free | Free | Download a library script, verify contents match source, re-upload | AUGMENT | `tests/e2e/free/controls/scripts/library.spec.ts` | Add a "download matches source" sub-test — mirrors config-profiles download sub-test. `ScriptsLibraryPage.downloadScript()` already exists; spec never calls it. Pair with #2 (tier split, not one spec). |
| 2 | controls-download-script-premium | Premium | Same as #1 under Unassigned scope | AUGMENT | `tests/e2e/premium/controls/scripts/library.spec.ts` | Same addition as #1. POM method exists. |
| 3 | controls-macos-custom-settings-add-profile-with-custom-labels | Premium | Upload a config profile targeted at a **Custom label**, verify applied-label filter shows "only applies to hosts that: > 20gb free" | NEW | `tests/e2e/premium/controls/os-settings/configuration-profiles.spec.ts` (add a case) or new `configuration-profiles-labels.spec.ts` | Existing config-profiles spec does upload/download/delete only — no label targeting. POM gap: `ConfigurationProfilesPage` upload modal needs Target=Custom + label-checkbox select, plus the row hover filter-icon → "applies to hosts that:" assertion. **Needs a label precondition** (a label like `> 20gb free`) — no label API/fixture helper exists in cluster. |
| 4 | controls-macos-setup-experience-check-install-fleetd-manually | Premium | Bootstrap → Advanced options → "Install fleetd manually": macOS Install-software tab + Run-script upload become disabled | NEW | `tests/e2e/premium/controls/setup-experience/bootstrap-fleetd-manual.spec.ts` | Genuine gap — not in bootstrap-package.spec.ts (upload/dl/delete) nor install-software.spec.ts. POM gap: `BootstrapPackagePage` needs `advancedOptions` button + `installManuallyCheckbox` + save; assert disabled state on `InstallSoftwarePage` (macOS tab) and `RunScriptPage` upload. |
| 5 | controls-macos-setup-experience-upload-bootstrap-package | Premium | Upload bootstrap .pkg, reload, assert it's listed + status table headers | DUP | `tests/e2e/premium/controls/setup-experience/bootstrap-package.spec.ts` | Existing spec already does upload → list → download → delete + API sha256/metadata (deeper). Only extra here = presence of `.bootstrap-package-table` status headers (Installed/Failed/Pending) which need enrolled hosts to be meaningful — low value; drop. |
| 6 | controls-macos-setup-ui-validation | Premium | Setup-exp Users: enable "Require IdP auth" + "Lock end user info", persist across reload; "Preview end user experience" opens preview tab; bootstrap docs link | AUGMENT | `tests/e2e/premium/controls/setup-experience/users.spec.ts` | users.spec already round-trips Require-IdP + hidden-admin toggles. Graft: **"Lock end user info" toggle round-trip** (verify it still exists in current UI) + **"Preview end user experience"** opens the `/setup-experience` preview tab. CUT the bootstrap-package "Learn more"/docs-link half (external docs). |
| 7 | controls-macos-updates-host-count-updates-correctly-if-host-team-changes | Premium | OS-updates host-count cell matches host-list count; updates after host transferred between teams (cron-triggered) | NEW (scoped) | `tests/e2e/premium/controls/os-updates/host-count.spec.ts` | Core = host-count cell → "View all hosts" → count parity (`OsUpdatesPage.viewHostsForFirstOs()` exists). **SCALE/FLAKE**: the "updates on team change" half needs host-transfer-between-teams + `/api/latest/fleet/trigger?name=cleanups_then_aggregation` + aggregation-timing waits + QA-Wolf teams (Ducks / "Team for macOS updates tests"). Recommend shipping the parity assertion on Workstations; CUT the transfer+cron half (violates no-team-create, aggregation flake). |
| 8 | controls-batch-run-scripts-on-hundreds-of-hosts-cancel-all-queued-if-modified | Premium | Run script on hundreds of hosts, then edit the script mid-run → queued runs auto-cancel; verify Canceled + Finished tabs | MERGE | proposed `tests/e2e/premium/controls/scripts/batch-progress-cancel.spec.ts` (group C) | **SCALE (hundreds of hosts)** — scale down to a handful. Distinct nuance worth keeping: editing a library script while a batch is queued cancels the queued runs (warning modal copy: "…it will complete, but results won't appear in Fleet."). Merge with #17. Needs live hosts + script execution. |
| 9 | scripts-batch-script-progress-page-is-accessible | Premium | Controls > Scripts > Batch progress nav; empty-state ("No batch scripts started") + Scheduled/Finished tabs + Library/Batch-progress links | NEW (cheap) | `tests/e2e/premium/controls/scripts/batch-progress.spec.ts` | No hosts / no execution needed — safe to author now. POM gap: `ScriptsBatchProgressPage` needs empty-state locator, "Batch progress" heading, Library/Batch-progress sub-nav links, Scheduled/Finished tab presence. |
| 10 | scripts-all-hosts-can-be-viewed-for-any-script-and-the-filter-dropdown-updates-the-list | Premium | On a batch's host list, the status filter dropdown (Ran/Error/Pending/Incompatible/Canceled) partitions all hosts (union == all, disjoint) | MERGE | proposed `tests/e2e/premium/controls/scripts/batch-progress-host-status.spec.ts` (group D) | **SCALE/FLAKE**: runs on many live hosts, waits for statuses to finalize. Merge with #12 + #18 (all are host-status-tab coverage). |
| 11 | scripts-correct-currently-running-scripts-are-displayed-on-the-started-tab | Premium | Two run batches show under Started; a not-run script doesn't | MERGE | proposed `tests/e2e/premium/controls/scripts/batch-progress-lifecycle.spec.ts` (group B) | **SCALE**: runs on ~50 hosts x2. Merge with #13 + #14 (run-now → Started → Finished lifecycle). |
| 12 | scripts-correct-hosts-are-displayed-under-each-tab-run-errored-pending-incompatible-canceled | Premium | Hosts map to correct status tab by OS (Windows=Incompatible, fedora=Errored, mac/linux=Ran); cancel moves Pending→Canceled | MERGE | group D (`batch-progress-host-status.spec.ts`) | **SCALE + heterogeneous-host infra gap**: needs one team with Windows + fedora + macOS/linux live hosts. Our gitops model = Unassigned/Workstations; no such mixed-OS team. Merge with #10 + #18. |
| 13 | scripts-finished-scripts-are-displayed-under-the-finished-tab | Premium | Run-now on a host → appears under Finished ("Completed less than a minute ago") | MERGE | group B (`batch-progress-lifecycle.spec.ts`) | **SCALE**: live host + up to 310s wait. Merge with #11 + #14. |
| 14 | scripts-run-now-still-functions-as-before-script-execution-is-sent-to-the-hosts | Premium | Run-now sends execution; host activity log shows the run after refetch | MERGE | group B (`batch-progress-lifecycle.spec.ts`) | **SCALE**: live host, refetch, up to 4min wait on host activity. Merge with #11 + #13. |
| 15 | scripts-running-scheduled-and-finished-scripts-can-be-previewed | Premium | Script-details ("preview") modal shows script contents across Scheduled/Started/Finished tabs | MERGE | proposed `tests/e2e/premium/controls/scripts/batch-progress-schedule-preview.spec.ts` (group E) | **SCALE + drop screenshots** (`toHaveScreenshot` violates suite style). Preview modal (`.script-details-modal`, textarea == script body) is worth capturing. Merge with #16. |
| 16 | scripts-schedule-for-later-allows-the-user-to-customize-the-time | Premium | Schedule a run 5 min out → Scheduled tab shows "Will start in 5 minutes" + tooltip time; later moves to Started at scheduled time | MERGE | group E (`batch-progress-schedule-preview.spec.ts`) | **SCALE/FLAKE**: up to 10-min real-time wait for the scheduled run to fire. Merge with #15. |
| 17 | scripts-script-execution-can-be-canceled-and-the-script-moves-to-the-correct-status | Premium | Run-now, cancel via confirm modal, verify script lands under Finished as "Canceled" | MERGE | group C (`batch-progress-cancel.spec.ts`) | **SCALE**: live host + up to 3-min wait. Merge with #8. |
| 18 | scripts-status-counts-on-batch-script-details-page-tabs | Premium | Per-tab count badges (`.tab-text__count`) match the tab's host-count header, during run and after cancel | MERGE | group D (`batch-progress-host-status.spec.ts`) | **SCALE (hundreds — "select all matching hosts" ~500)**: scale down. Count-badge-vs-header assertion is the useful bit. Merge with #10 + #12. |
| 19 | secrets-run-script-modal-now-contains-two-options-run-now-and-schedule-for-later | Premium | Batch Run-script modal shows Run now / Schedule for later radios; toggling shows Date/Time (UTC) fields | NEW (cheap) | `tests/e2e/premium/controls/scripts/run-script-modal.spec.ts` | Mis-filed as "secrets" — it's the batch run-script modal UI. Only needs 1 host selected (no execution). POM gap: new `RunScriptBatchModal` component (script picker rows, Run-now/Schedule radios, Date/Time UTC textboxes, Run button). Seeds the batch cluster's modal POM. |
| 20 | secrets-scripts-with-a-secret-variable-can-only-be-uploaded-when-such-variable-exists | Premium | Uploading a script referencing `$FLEET_SECRET_X` fails until the custom variable X exists | MERGE | proposed `tests/e2e/premium/controls/variables/script-secret-dependency.spec.ts` | Toast copy: `Couldn't add. Variable "$FLEET_SECRET_<X>" doesn't exist.` then success once created. Merge with #22 (both = script↔variable dependency). Needs a script fixture that references `$FLEET_SECRET_`. |
| 21 | secrets-secrets-via-ui-custom-variables-can-be-added-saved-deleted-and-field-validation | Premium | Custom-variable CRUD + field validation (required, uppercase-forced, special-char rejection, disabled Save) | NEW (big) | `tests/e2e/premium/controls/variables/custom-variables.spec.ts` | Core VariablesPage CRUD spec. `VariablesPage` POM is currently goto-only — needs full buildout: Add-custom-variable modal, Name/Value fields, validation messages, Save enable/disable, list item (`$FLEET_SECRET_<name>`), delete confirm modal + toasts ("Variable created" / "Variable successfully deleted."). **Drop the `toHaveScreenshot` delete-modal assertion.** |
| 22 | secrets-variable-that-is-referenced-by-a-script-can-not-be-deleted | Premium | Deleting a variable used by a library script fails with a helpful toast; variable stays | MERGE | `tests/e2e/premium/controls/variables/script-secret-dependency.spec.ts` (with #20) | Toast: `Couldn't delete. <VAR> is used by the "<script>" script in the "<team>" team. Please edit or delete the script and try again.` Uses QA-Wolf "Virtual Machines" team + a settings toggle to enable scripts — remap to Unassigned/Workstations; the referenced-var check does **not** require hosts (just a library script referencing the var). Merge with #20. |
| 23 | packages-edit-packages-edit-advanced-attributes | Premium | Software Edit → Advanced options: edit preinstall query + install/postinstall/uninstall scripts, save through confirm modal, re-open to verify persistence | MERGE | proposed `tests/e2e/premium/software/edit-package.spec.ts` (serial) | **NEW area** — software library.spec.ts is add+delete only ("no in-UI edit step"). POM gap: an `EditSoftwareModal` POM (Actions → Edit software → Advanced options; `#preInstallQuery`, `#install-script`, `#post-install-script-editor`, `#uninstall-script-editor` editors; "Save changes?" confirm). Merge with #24. Precondition: a custom .pkg uploaded via `uploadSoftwarePackage`. |
| 24 | packages-edit-packages-self-service | Premium | Software Edit → toggle Self-service switch, save through confirm modal, verify chip + persistence | MERGE | `tests/e2e/premium/software/edit-package.spec.ts` (with #23) | Same Edit modal + confirm modal. Self-service ON toast adds "The end user can install from Fleet Desktop." + a `Self-service` chip. Note: `SoftwareLibraryPage.selfServiceSwitch` exists but is the **add-form** switch, not the edit modal. Merge with #23. |

## Summary

- Counts: DUP **1**, AUGMENT **3**, NEW **6**, CUT **0**, MERGE **14**
  - (CUT appears only as sub-parts folded into AUGMENT/NEW rows: #5 status-table headers, #6 bootstrap docs-link, #7 team-change+cron half.)

- **NEW specs recommended** (safe / low-scale first):
  - `tests/e2e/premium/controls/scripts/batch-progress.spec.ts` — batch-progress nav + empty-state (#9). No hosts. Extend `ScriptsBatchProgressPage`.
  - `tests/e2e/premium/controls/scripts/run-script-modal.spec.ts` — Run-now / Schedule-for-later modal UI (#19). 1 host, no execution. New `RunScriptBatchModal` component.
  - `tests/e2e/premium/controls/variables/custom-variables.spec.ts` — custom-variable CRUD + validation (#21). Big `VariablesPage` buildout.
  - `tests/e2e/premium/controls/setup-experience/bootstrap-fleetd-manual.spec.ts` — "Install fleetd manually" disables macOS install-software + run-script (#4).
  - `tests/e2e/premium/controls/os-settings/configuration-profiles*.spec.ts` — custom-label-targeted profile case (#3). Needs a label precondition.
  - `tests/e2e/premium/controls/os-updates/host-count.spec.ts` — host-count → View-all-hosts parity (#7, scoped; drop the team-change/cron half).
  - MERGE specs: `.../variables/script-secret-dependency.spec.ts` (#20+#22), `tests/e2e/premium/software/edit-package.spec.ts` (#23+#24), and the four **scale-gated** batch specs below.

- **Scale/flake-gated MERGE specs (live-host batch cluster — do NOT author until host infra is confirmed):**
  - group B `batch-progress-lifecycle.spec.ts` (#11+#13+#14) — run-now → Started → Finished.
  - group C `batch-progress-cancel.spec.ts` (#8+#17) — cancel + cancel-queued-on-edit.
  - group D `batch-progress-host-status.spec.ts` (#10+#12+#18) — per-status host tabs + count badges + filter. Needs a **mixed-OS host team** (Win=Incompatible, fedora=Errored, mac/linux=Ran).
  - group E `batch-progress-schedule-preview.spec.ts` (#15+#16) — schedule-for-later + script-details preview modal (drop screenshots).

- **AUGMENTs:** scripts library download-matches-source (#1 free / #2 premium; POM method already exists); setup-experience Users "Lock end user info" toggle + "Preview end user experience" (#6).

- **Notable CUTs (folded in):** #5 upload-bootstrap → DUP of existing bootstrap-package.spec.ts (status-table headers need hosts, low value); #7 team-change/cron-aggregation half (host transfer + `cleanups_then_aggregation` trigger = flake + no-team-create violation); #6 bootstrap docs "Learn more" link; all QA-Wolf screenshot assertions (#15, #21) dropped per suite style.

- **POM / helper work required:**
  - **New component POMs:** `RunScriptBatchModal` (script picker + Run-now/Schedule radios + Date/Time UTC + Run); `BatchScriptDetailsPage` (per-status host tables Ran/Errored/Pending/Incompatible/Canceled, `.tab-text__count` badges, Cancel + confirm modal, "View all hosts", script-details/preview modal).
  - **Extend existing POMs:** `ScriptsBatchProgressPage` (empty state, list items w/ status+timestamp, click-into-batch, Library/Batch-progress subnav); `VariablesPage` (add modal, name/value fields, validation, save, list item, delete modal — currently goto-only); `BootstrapPackagePage` (Advanced options + install-fleetd-manually checkbox + save); `ConfigurationProfilesPage` (Target=Custom label select + applied-labels filter view); new `EditSoftwareModal` (advanced-options editors + self-service switch).
  - **API helpers MISSING (QA Wolf leaned on non-existent utils):** custom-variable CRUD/cleanup (`createCustomVariable`/`cleanupCustomVariables`), script upload/delete via API (no script API helper today — only UI POM), a label precondition helper, and a `cleanups_then_aggregation` cron-trigger helper (only if #7 team-change is ever pursued).
  - Script fixtures needed: one referencing `$FLEET_SECRET_<VAR>` (for #20/#22).

- **Role-model / infra gaps:**
  - All flows run as global-admin → static-users covers it; no missing role.
  - **Live-host dependency** is the dominant blocker: the batch-progress lifecycle/cancel/status/schedule flows need enrolled hosts actually executing scripts, real-time waits (up to 10 min), and a **heterogeneous-OS team** (Windows + fedora + macOS) that our gitops model (Unassigned/Workstations) doesn't provide. QA-Wolf used ad-hoc teams (Ducks, Virtual Machines, "Team for macOS updates tests") + host transfer + team create — all forbidden here; must remap to Workstations/Unassigned.
  - "Hundreds of hosts" (#8, #18) is loadtest territory, not available on the functional premium instance — scale down to a handful.

- **Open questions for the human:**
  1. Do we have (or can gitops provision) a persistent host team with a Windows + fedora + macOS/linux mix for the per-status batch coverage (#10/#12/#18)? Otherwise those stay manual-QA.
  2. Batch "hundreds of hosts" — scale down to N live hosts for functional coverage, or leave to loadtest/manual?
  3. Should custom variables (secrets) get API precondition/cleanup helpers, or be driven purely through the UI POM?
  4. Is the "Lock end user info" toggle still present on the setup-experience Users tab (current suite's users.spec knows only Require-IdP + hidden-admin)? Confirm before augmenting #6.
  5. Confirm we drop all `toHaveScreenshot` visual assertions (#15 preview, #21 delete-secret modal) in favor of DOM assertions.
  6. Do the long-running/scheduled batch specs belong in the normal premium project or should they be gated like the existing host-dependent tests?
