
# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.auth/
.perf-results/
.perf-history/

# Per-run triage write-ups produced by the playwright-run-reviewer skill.
# Each one is a snapshot of a single CI run; the durable conclusions get
# promoted into TODO.md or docs/blocked-by-product-bugs.md, so the raw
# reviews stay local.
docs/run-reviews/

# Pre-upgrade impact reports from the fleet-upgrade-preflight skill. Derived
# from two Fleet commits and regenerated in seconds, so the report itself is
# disposable — anything worth keeping goes into the POM fix or TODO.md.
docs/upgrade-preflight/

# Claude Code local settings (the skills tree beside it is checked in)
.claude/settings.local.json

# Local environment — never commit real credentials
.env
.env.premium
.env.free
.env.loadtest
