# OS / editor junk
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
*.swp
*.swo

# Claude Code local settings
.claude/settings.local.json

# Logs
*.log

# Playwright-suite ignores live in playwright/.gitignore

# Skip the auth + loadtest test directories (kept local). Scoped to
# playwright/tests/ so we don't shadow playwright/pages/auth/ which holds
# real page-object source.
playwright/tests/auth/
playwright/tests/loadtest/